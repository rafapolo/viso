name: Build and Deploy with Rolldown

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build with Rolldown
      run: npm run build
      env:
        NODE_ENV: production

    - name: Check bundle sizes
      run: |
        echo "📦 Bundle Analysis:"
        ls -lah dist/
        echo ""
        echo "🗂️ Apps bundle size:"
        du -h dist/js/apps-*.js || echo "Apps bundle not found"
        echo ""
        echo "🗂️ Core bundle size:"
        du -h dist/js/core-*.js || echo "Core bundle not found"
        echo ""
        echo "🎨 CSS size:"
        du -h dist/css/main-*.css || echo "CSS file not found"
        echo ""
        echo "📊 Assets:"
        du -h dist/despesas.parquet || echo "Parquet file not found"

    - name: Performance analysis
      run: |
        echo "⚡ Performance Metrics:"
        echo "Build completed in $(date)"
        echo "Total dist size: $(du -sh dist/ | cut -f1)"
        
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build-output-${{ matrix.node-version }}
        path: dist/
        retention-days: 30

    - name: Run basic functionality tests
      run: |
        echo "🧪 Basic Tests:"
        echo "Checking if main bundles exist..."
        ls dist/js/apps-*.js > /dev/null 2>&1 && echo "✅ apps.js exists" || echo "❌ apps.js missing"
        ls dist/js/core-*.js > /dev/null 2>&1 && echo "✅ core.js exists" || echo "❌ core.js missing"
        test -f dist/index.html && echo "✅ index.html exists" || echo "❌ index.html missing"
        test -f dist/db.html && echo "✅ db.html exists" || echo "❌ db.html missing"
        ls dist/css/main-*.css > /dev/null 2>&1 && echo "✅ main.css exists" || echo "❌ main.css missing"

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    permissions:
      contents: read
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build for production
      run: npm run build
      env:
        NODE_ENV: production

    - name: Setup GitHub Pages
      uses: actions/configure-pages@v4

    - name: Upload to GitHub Pages
      uses: actions/upload-pages-artifact@v3
      with:
        path: './dist'

    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4

  performance-monitoring:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build and measure
      run: |
        echo "🚀 Starting build measurement..."
        START_TIME=$(date +%s)
        npm run build
        END_TIME=$(date +%s)
        BUILD_TIME=$((END_TIME - START_TIME))
        
        echo "⏱️ Build completed in ${BUILD_TIME} seconds"
        echo "BUILD_TIME=${BUILD_TIME}" >> $GITHUB_ENV
        
        # Bundle analysis
        APPS_SIZE=$(stat --format=%s dist/js/apps-*.js 2>/dev/null | head -n1 || echo "0")
        CORE_SIZE=$(stat --format=%s dist/js/core-*.js 2>/dev/null | head -n1 || echo "0")
        CSS_SIZE=$(stat --format=%s dist/css/main-*.css 2>/dev/null | head -n1 || echo "0")
        TOTAL_SIZE=$((APPS_SIZE + CORE_SIZE + CSS_SIZE))
        
        echo "📊 Bundle Sizes:"
        echo "  - Apps: $(numfmt --to=iec ${APPS_SIZE})"
        echo "  - Core: $(numfmt --to=iec ${CORE_SIZE})"
        echo "  - CSS: $(numfmt --to=iec ${CSS_SIZE})"
        echo "  - Total JS+CSS: $(numfmt --to=iec ${TOTAL_SIZE})"
        
        # Store metrics
        echo "APPS_SIZE=${APPS_SIZE}" >> $GITHUB_ENV
        echo "CORE_SIZE=${CORE_SIZE}" >> $GITHUB_ENV
        echo "CSS_SIZE=${CSS_SIZE}" >> $GITHUB_ENV
        echo "TOTAL_SIZE=${TOTAL_SIZE}" >> $GITHUB_ENV

    - name: Comment on PR with metrics
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v7
      with:
        script: |
          const buildTime = process.env.BUILD_TIME;
          const appsSize = parseInt(process.env.APPS_SIZE);
          const coreSize = parseInt(process.env.CORE_SIZE);
          const cssSize = parseInt(process.env.CSS_SIZE);
          const totalSize = parseInt(process.env.TOTAL_SIZE);
          
          function formatBytes(bytes) {
            if (bytes === 0) return '0 B';
            const k = 1024;
            const sizes = ['B', 'KB', 'MB', 'GB'];
            const i = Math.floor(Math.log(bytes) / Math.log(k));
            return parseFloat((bytes / Math.pow(k, i)).toFixed(2)) + ' ' + sizes[i];
          }
          
          const comment = `## 📊 Rolldown Build Performance
          
          ### ⚡ Build Metrics
          - **Build Time**: ${buildTime}s
          - **Total Bundle Size**: ${formatBytes(totalSize)}
          
          ### 📦 Bundle Breakdown
          | Bundle | Size | 
          |--------|------|
          | apps.js | ${formatBytes(appsSize)} |
          | core.js | ${formatBytes(coreSize)} |
          | main.css | ${formatBytes(cssSize)} |
          
          ### 🚀 Performance Benefits
          - ✅ Rust-based bundling with Rolldown
          - ✅ Eliminated CDN dependencies  
          - ✅ Tree-shaking optimization
          - ✅ Production-ready minification
          
          Built with Rolldown 🦀`;
          
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: comment
          });

    - name: Archive performance metrics
      uses: actions/upload-artifact@v4
      with:
        name: performance-metrics
        path: |
          dist/
        retention-days: 7